"""
This file is a template. To turn it into a solution, remove these lines and update the link and any key information from the problem statement.
Then, delete any solutions which are not needed, and update the docstrings with the arguments, return values, complexities, and any other relevant information.

Answer to: https://leetcode.com/problems/

Note that the problem statement guarantees that ___________.
"""


def solution_1(integer_array: list[int], target: int) -> int:
    """
    This is the optimal solution to the problem.
    It is O(1) time complexity and O(1) space complexity.
    This is due to ___________ and ___________.
    This solution uses ___________'s algorithm to ___________.

    Args:
        integer_array (list[int]): A sorted list of integers to search.
        target (int): The target value to search for.

    Returns:
        int: The index of the target value in the list, or -1 if the target value is not found.
    """
    pass


def solution_2(integer_array: list[int], target: int) -> int:
    """
    This is a time-optimized solution to the problem.
    It is O(1) time complexity and O(n) space complexity.
    This is due to ___________ and ___________.
    This solution uses ___________'s algorithm to ___________.

    Args:
        integer_array (list[int]): A sorted list of integers to search.
        target (int): The target value to search for.

    Returns:
        int: The index of the target value in the list, or -1 if the target value is not found.
    """
    pass


def solution_3(integer_array: list[int], target: int) -> int:
    """
    This is a space-optimized solution to the problem.
    It is O(n) time complexity and O(1) space complexity.
    This is due to ___________ and ___________.
    This solution uses ___________'s algorithm to ___________.

    Args:
        integer_array (list[int]): A sorted list of integers to search.
        target (int): The target value to search for.

    Returns:
        int: The index of the target value in the list, or -1 if the target value is not found.
    """
    pass
